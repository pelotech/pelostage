apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: pelostage
  description: The pelotech backstage application repo
  # Example for optional annotations
  annotations:
    github.com/project-slug: pelotech/pelostage
    backstage.io/techdocs-ref: dir:.
spec:
  type: website
  owner: leads
  lifecycle: production
